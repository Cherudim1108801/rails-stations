<!DOCTYPE html>
<html>
    <head>
        <title>上映映画一覧</title>
        <meta charset="UTF-8">
    </head>
    <body>
        <h1>上映映画一覧</h1>
        <div>
            <%= button_to "新規", {controller: 'admin/movies', action: 'new'}, {method: :get} %>
        </div>
        <br>
        <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>タイトル</th>
                <th>公開年</th>
                <th>概要</th>
                <th>画像URL</th>
                <th>上映中</th>
                <th>登録日時</th>
                <th>更新日時</th>
            </tr>
        </thead>
            <% @movies.each do |movie|%>
            <tr>
                <th><a href="/admin/movies/<%=movie.id%>/edit"><%=movie.id%></a></th>
                <th><%=movie.name%></th>
                <th><%=movie.year%></th>
                <th><%=movie.description%></th>
                <th>
                    <a href="<%=movie.image_url%>">画像</a>
                </th>
                <th><%=movie.is_showing ? "上映中" : "上映予定"%></th>
                <th><%=movie.created_at%></th>
                <th><%=movie.updated_at%></th>
            </tr>
            <% end %>
        </table>
    </body>
</html>